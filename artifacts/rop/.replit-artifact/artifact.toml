kind = "web"
previewPath = "/"
title = "Rise of the Plants"
version = "1.0.0"
id = "artifacts/rop"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25296

[services.development]
run = "pnpm --filter @workspace/rop run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rop", "run", "build" ]
publicDir = "artifacts/rop/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25296"
BASE_PATH = "/"
